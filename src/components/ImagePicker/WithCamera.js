import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const WithCamera = () => {
  return (
    <View>
      <Text style={{color: 'black'}}>WithCamera</Text>
    </View>
  );
};

export default WithCamera;

const styles = StyleSheet.create({});
